<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ex="clr-namespace:policy.app.Controls"
             x:Class="policy.app.Pages.SearchPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <StackLayout Margin="40">
                    <Label TextColor="#228bcc" FontSize="35" Text="Поиск" HorizontalOptions="Center" FontFamily="CormorantGaramondRegular.ttf#CormorantGaramondRegular"/>
                </StackLayout>
                <StackLayout Margin="15,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ex:SearchEntry Grid.Column="0" Placeholder="Поиск"  Margin="15"/>
                        <Button Grid.Column="1" Text="Найти" BackgroundColor="#228bcc" TextColor="White" BorderColor="#19365f" BorderWidth="1" CornerRadius="5" Padding="5" HeightRequest="40"/>
                    </Grid>
                    <BoxView HorizontalOptions="Fill" Color="Black" HeightRequest="3" Margin="15"/>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>