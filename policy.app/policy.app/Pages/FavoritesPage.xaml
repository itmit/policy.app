<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewCell="clr-namespace:policy.app.Views.ViewCell"
             x:Class="policy.app.Pages.FavoritesPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Margin="0,5,0,0">

                <ListView ItemsSource="{Binding Gophers}" HasUnevenRows="True" SelectedItem="{Binding SelectedGopher}" ItemSelected="ListView_OnItemSelected">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <viewCell:FavoriteUserViewCell/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>