<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewCell="clr-namespace:policy.app.Views.ViewCell"
             xmlns:ex="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             xmlns:controls="clr-namespace:policy.app.Controls"
             x:Class="policy.app.Pages.MenuPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal" Margin="35,20,0,20">
                <ex:CircleImage Source="avatar.png" Margin="0,0,15,0" Aspect="AspectFill" WidthRequest="65" HeightRequest="65"/>
                <Label Text="Пестель Яна" TextColor="#19365f" VerticalOptions="Center" FontAttributes="Bold" FontSize="30"/>
            </StackLayout>
            <ListView x:Name="ll" SelectionMode="Single" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" ItemsSource="{Binding MenuCollection}" HasUnevenRows="True" SeparatorColor="#19365f" ItemSelected="Ll_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <viewCell:MenuViewCell/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>