<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ex="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             xmlns:viewCells="clr-namespace:policy.app.Views.ViewCells"
             xmlns:forms="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:transformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations"
             x:Class="policy.app.Pages.MenuPage">
    <ContentPage.Content>
        <StackLayout>
            <Grid Margin="35,20,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <forms:CachedImage Grid.Row="0" Grid.Column="0" Source="{Binding UserPicture}" Margin="0,0,15,0" Aspect="AspectFill" WidthRequest="65"
                                HeightRequest="65" >
                    <forms:CachedImage.Transformations>
                        <transformations:RoundedTransformation Radius="200"/>
                    </forms:CachedImage.Transformations>
                </forms:CachedImage>
                <Label Grid.Row="1" Grid.Column="1" Text="{Binding UserName}" TextColor="#19365f" VerticalOptions="Center" FontAttributes="Bold"
                       FontSize="30" />
            </Grid>
            <ListView x:Name="ll" SelectionMode="Single" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      ItemsSource="{Binding MenuCollection}" HasUnevenRows="True" SeparatorColor="#19365f"
                      ItemSelected="Ll_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <viewCells:MenuViewCell />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>