<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:abstractions="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             x:Class="policy.app.Views.ViewCells.SearchUserViewCell">
  <ViewCell.View>
        <StackLayout>
            <Grid Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <abstractions:CircleImage Grid.Column="0" Margin="5" Source="User_def.png" Aspect="AspectFill"
                                 VerticalOptions="Start" HorizontalOptions="CenterAndExpand" WidthRequest="50"
                                 HeightRequest="50" />
                <Label Grid.Column="1" Margin="5,0,5,0" Text="{Binding Name}" VerticalOptions="Center"
                       FontSize="Medium" />
            </Grid>
            <BoxView HeightRequest="2" BackgroundColor="#dee4ea" />
        </StackLayout>
  </ViewCell.View>
</ViewCell>