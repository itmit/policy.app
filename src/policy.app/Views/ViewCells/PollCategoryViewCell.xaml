<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="policy.app.Views.ViewCells.PollCategoryViewCell">
    <ViewCell.View>
        <StackLayout>
            <StackLayout Orientation="Horizontal" Margin="20,25,20,20">
                <Image Source="{Binding ImageSource}" WidthRequest="50" HeightRequest="50" />
                <Label Text="{Binding Name}" TextColor="#228bcc" FontSize="25" Margin="8,0,0,0" FontAttributes="Bold"
                       VerticalOptions="Center" />
            </StackLayout>
            <BoxView HeightRequest="1" BackgroundColor="#dee4ea" />
        </StackLayout>
    </ViewCell.View>
</ViewCell>