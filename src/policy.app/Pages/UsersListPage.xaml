<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewCells="clr-namespace:policy.app.Views.ViewCells"
             xmlns:views="clr-namespace:policy.app.Views;assembly=policy.app"
             Title="{Binding Title}"
             x:Class="policy.app.Pages.UsersListPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <StackLayout Margin="0,5,0,0">
                    <StackLayout Margin="0,5,0,0"
                                 BindableLayout.ItemsSource="{Binding Users}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <viewCells:UserViewCell />
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </StackLayout>
                </StackLayout>

                <StackLayout>
                    <SearchBar Placeholder="Поиск" Margin="15" Text="{Binding Query}"/>
                    <BoxView HeightRequest="2" BackgroundColor="#dee4ea" />
                </StackLayout>
                <StackLayout Margin="0,5,0,0">
                    <StackLayout Margin="0,5,0,0"
                                 BindableLayout.ItemsSource="{Binding Users}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                    <views:UserView>
                                        <views:UserView.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="TapGestureRecognizer_OnTapped"/>
                                        </views:UserView.GestureRecognizers>
                                    </views:UserView>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </StackLayout>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>