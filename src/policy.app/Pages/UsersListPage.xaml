<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewCells="clr-namespace:policy.app.Views.ViewCells"
             Title="{Binding Title}"
             x:Class="policy.app.Pages.UsersListPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <SearchBar Placeholder="Поиск" Margin="15" Text="{Binding Query}"/>
                <BoxView HeightRequest="2" BackgroundColor="#dee4ea" />
            </StackLayout>
            <StackLayout Margin="0,5,0,0">
                <ListView HasUnevenRows="True" ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedGopher}"
                              ItemSelected="ListView_OnItemSelected">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <viewCells:UserViewCell />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>