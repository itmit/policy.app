<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewCells="clr-namespace:policy.app.Views.ViewCells;assembly=policy.app"
             mc:Ignorable="d"
             BackgroundImageSource="background"
             x:Class="policy.app.Pages.PollPage"
             Title="Опросы">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding PollCategories}" HasUnevenRows="True" SelectedItem="{Binding SelectedPollCategory}"
                      ItemSelected="ListView_OnItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <viewCells:PollCategoryViewCell/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>